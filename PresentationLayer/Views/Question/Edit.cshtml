@model BusinessLogicLayer.DTOs.QuizDtos.QuestionRequest

@{
    ViewData["Title"] = "Edit Question";
    Layout = "_Layout2";
}

@section CustomHeadContent {
    <link rel="stylesheet" href="~/css/quiz.css">
}

<div class="quiz-container">
    <div class="quiz-breadcrumb animate-fade-in">
        <a asp-controller="Quiz" asp-action="AdminIndex">Quizzes</a>
        <span class="quiz-breadcrumb-separator">•</span>
        <a asp-action="Index" asp-route-quizId="@Model.QuizID">Questions</a>
        <span class="quiz-breadcrumb-separator">•</span>
        <span>Edit Question</span>
    </div>

    <div class="quiz-header">
        <h1 class="quiz-title animate-fade-in">Edit Question</h1>
    </div>
    
    <div class="quiz-content animate-fade-in animate-delay-100">
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="quiz-alert quiz-alert-error">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"></circle>
                    <line x1="12" y1="8" x2="12" y2="12"></line>
                    <line x1="12" y1="16" x2="12.01" y2="16"></line>
                </svg>
                <p>@TempData["ErrorMessage"]</p>
            </div>
        }
        
 
        <div class="quiz-main">
            <form asp-action="Edit" method="post" class="quiz-form" id="editQuestionForm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                
                <!-- Essential hidden fields -->
                <input type="hidden" asp-for="QuestionID" id="QuestionID" value="@Model.QuestionID" />
                <input type="hidden" asp-for="QuizID" id="QuizID" value="@Model.QuizID" />
                <input type="hidden" asp-for="RightAns" id="RightAns" value="@Model.RightAns" />
                
                <div class="form-group">
                    <label asp-for="QuestionText" class="form-label">Question Text</label>
                    <textarea asp-for="QuestionText" class="form-textarea" rows="3" required></textarea>
                    <span asp-validation-for="QuestionText" class="text-danger"></span>
                </div>
                
                <div class="form-group">
                    <label asp-for="Score" class="form-label">Score</label>
                    <input asp-for="Score" class="form-input" type="number" min="1" required />
                    <span asp-validation-for="Score" class="text-danger"></span>
                </div>
                
                <div class="form-group">
                    <label class="form-label">Answer Options</label>
                    <div class="answer-options-container" style="background-color: var(--brand-teal-light); padding: 1.5rem; border-radius: var(--border-radius);">
                        <div class="quiz-alert quiz-alert-info" style="margin-bottom: 1rem;">
                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <circle cx="12" cy="12" r="10"></circle>
                                <line x1="12" y1="16" x2="12" y2="12"></line>
                                <line x1="12" y1="8" x2="12.01" y2="8"></line>
                            </svg>
                            <div>
                                <p style="margin-bottom: 0.5rem;"><strong>Instructions:</strong></p>
                                <ol style="margin-left: 1.5rem; margin-bottom: 0;">
                                    <li>Fill in at least one answer option</li>
                                    <li>Select the radio button next to the correct answer</li>
                                    <li>Empty answer fields will be ignored</li>
                                    <li>Maximum 200 characters per answer</li>
                                </ol>
                            </div>
                        </div>
                        
                        <div id="answer-options">
                            @if (Model.Answers != null && Model.Answers.Any())
                            {
                                for (int i = 0; i < Model.Answers.Count; i++)
                                {
                                    <div class="answer-option" style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1rem;">
                                        <div class="answer-radio" style="flex-shrink: 0;">
                                            <input type="radio" id="rightAnswer_@i" name="rightAnswerRadio" value="@i" 
                                                @(Model.Answers[i] == Model.RightAns ? "checked" : "") required
                                                style="width: 1.25rem; height: 1.25rem; cursor: pointer;">
                                        </div>
                                        <div style="flex-grow: 1;">
                                            <label for="rightAnswer_@i" style="width: 100%;">
                                                <input name="Answers[@i]" id="Answers_@i" class="form-input answer-input" 
                                                       value="@Model.Answers[i]" placeholder="Answer option @(i+1)" maxlength="200" />
                                            </label>
                                        </div>
                                    </div>
                                }
                            }
                            else
                            {
                                <!-- Create default answer fields as fallback -->
                                <div class="quiz-alert quiz-alert-warning">
                                    <p>No answers found. Creating default answer options.</p>
                                </div>
                                
                                @for (int i = 0; i < 4; i++)
                                {
                                    <div class="answer-option" style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1rem;">
                                        <div class="answer-radio" style="flex-shrink: 0;">
                                            <input type="radio" id="rightAnswer_@i" name="rightAnswerRadio" value="@i" required 
                                                   style="width: 1.25rem; height: 1.25rem; cursor: pointer;">
                                        </div>
                                        <div style="flex-grow: 1;">
                                            <label for="rightAnswer_@i" style="width: 100%;">
                                                <input name="Answers[@i]" id="Answers_@i" class="form-input answer-input" 
                                                       placeholder="Answer option @(i+1)" value="" maxlength="200" />
                                            </label>
                                        </div>
                                    </div>
                                }
                            }
                        </div>
                    </div>
                </div>
                
                <div class="quiz-actions">
                    <a asp-action="Index" asp-route-quizId="@Model.QuizID" class="quiz-btn quiz-btn-secondary">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <line x1="19" y1="12" x2="5" y2="12"></line>
                            <polyline points="12 19 5 12 12 5"></polyline>
                        </svg>
                        Back to Questions
                    </a>
                    <button type="submit" class="quiz-btn quiz-btn-primary">
                        Save Changes
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section CustomScripts {
    <script>
        $(document).ready(function() {
            // Animations
            const fadeElements = document.querySelectorAll('.animate-fade-in');
            fadeElements.forEach(function(element) {
                element.classList.add('visible');
            });
            
            // Set initial selection based on right answer
            const rightAns = '@Model.RightAns';
            let foundMatch = false;
            
            if (rightAns) {
                $('.answer-input').each(function(index) {
                    const value = $(this).val().trim();
                    if (value && value === rightAns) {
                        $(`#rightAnswer_${index}`).prop('checked', true);
                        foundMatch = true;
                    }
                });
            }
            
            // If no match was found and there are valid answers, select the first one
            if (!foundMatch) {
                let firstValidIndex = -1;
                $('.answer-input').each(function(index) {
                    if ($(this).val().trim() && firstValidIndex === -1) {
                        firstValidIndex = index;
                    }
                });
                
                if (firstValidIndex !== -1) {
                    $(`#rightAnswer_${firstValidIndex}`).prop('checked', true);
                    const answerText = $(`#Answers_${firstValidIndex}`).val().trim();
                    $('#RightAns').val(answerText);
                }
            }
            
            // Function to update the RightAns value based on selected radio
            function updateRightAnswer() {
                const selectedRadio = $('input[name="rightAnswerRadio"]:checked');
                if (selectedRadio.length > 0) {
                    const selectedIndex = selectedRadio.val();
                    const answerText = $(`#Answers_${selectedIndex}`).val().trim();
                    if (answerText) {
                        $('#RightAns').val(answerText);
                    }
                }
            }
            
            // Track selected answer when radio changes
            $('input[name="rightAnswerRadio"]').change(function() {
                updateRightAnswer();
            });
            
            // Update RightAns when answer text changes
            $('.answer-input').on('input', function() {
                const inputId = $(this).attr('id');
                const index = inputId.split('_')[1];
                
                if ($(`#rightAnswer_${index}`).is(':checked')) {
                    $('#RightAns').val($(this).val().trim());
                }
            });
            
            // Form submission
            $('#editQuestionForm').submit(function(e) {
                // Make sure question text is provided
                if (!$('#QuestionText').val().trim()) {
                    alert('Please enter question text');
                    $('#QuestionText').focus();
                    e.preventDefault();
                    return false;
                }
                
                // Make sure score is valid
                const score = $('#Score').val();
                if (!score || parseInt(score) < 1) {
                    alert('Please enter a valid score (minimum 1)');
                    $('#Score').focus();
                    e.preventDefault();
                    return false;
                }
                
                // Check if any answers are provided
                let validAnswers = [];
                $('.answer-input').each(function() {
                    if ($(this).val().trim()) {
                        validAnswers.push({
                            index: $(this).attr('id').split('_')[1],
                            text: $(this).val().trim()
                        });
                    }
                });
                
                if (validAnswers.length === 0) {
                    alert('Please provide at least one answer option');
                    $('.answer-input:first').focus();
                    e.preventDefault();
                    return false;
                }
                
                // Make sure a correct answer is selected
                const selectedRadio = $('input[name="rightAnswerRadio"]:checked');
                if (selectedRadio.length === 0) {
                    // Auto-select the first valid answer
                    const firstValidIndex = validAnswers[0].index;
                    $(`#rightAnswer_${firstValidIndex}`).prop('checked', true);
                    $('#RightAns').val(validAnswers[0].text);
                } else {
                    // Update RightAns based on selected radio
                    const selectedIndex = selectedRadio.val();
                    const answerText = $(`#Answers_${selectedIndex}`).val().trim();
                    if (answerText) {
                        $('#RightAns').val(answerText);
                    } else {
                        // If the selected answer is empty, update to first valid answer
                        const firstValidIndex = validAnswers[0].index;
                        $(`#rightAnswer_${firstValidIndex}`).prop('checked', true);
                        $('#RightAns').val(validAnswers[0].text);
                    }
                }
                
                // Final check on RightAns to make sure it's not empty
                if (!$('#RightAns').val()) {
                    $('#RightAns').val(validAnswers[0].text);
                }
                
                console.log('Form data:');
                console.log('Question ID:', $('#QuestionID').val());
                console.log('Quiz ID:', $('#QuizID').val());
                console.log('Question Text:', $('#QuestionText').val());
                console.log('Score:', $('#Score').val());
                console.log('RightAns:', $('#RightAns').val());
                
                // Let the form submit naturally
            });
        });
    </script>
    
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
